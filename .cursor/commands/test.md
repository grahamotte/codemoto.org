Run `mise test` and fix any issues. Update the tests only, not the code.

If you strongly think that the code should be modified and not the test, because of some sort of clear logic, code, or setup issue, then ask me first before changing the code.

You may go ahead and fix rubocop or other linting/formatting errors without asking.
